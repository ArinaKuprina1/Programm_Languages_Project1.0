expr        : term ((PLUS|MINUS) term)*

term        : factor ((MUL|DIV) factor)*
            : factor ((OR|AND) factor)*
            : (NOT)factor
comparison  : term ((LT| GT| LTE| GTE| EQ| NEQ)term)*

factor      : INT|FLOAT
            : TRUE|FALSE
				    : (PLUS|MINUS) factor
			    	:  LPAREN expr RPAREN
